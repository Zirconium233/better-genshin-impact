<ui:FluentWindow x:Class="BetterGenshinImpact.View.Windows.ScriptSelectionDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:emoji="clr-namespace:Emoji.Wpf;assembly=Emoji.Wpf"
                 Width="500"
                 Height="600"
                 MinWidth="450"
                 MinHeight="500"
                 ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
                 ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                 ExtendsContentIntoTitleBar="True"
                 FontFamily="{DynamicResource TextThemeFontFamily}"
                 ResizeMode="CanResize"
                 WindowBackdropType="Mica"
                 WindowStartupLocation="CenterOwner"
                 mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Title="AI环境 - 脚本调度器" Grid.Row="0">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="pack://application:,,,/Resources/Images/logo.png" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <StackPanel Grid.Row="1" Margin="20">
            <!-- 标题和说明 -->
            <emoji:TextBlock Text="⚔️ 战斗脚本选择" 
                           FontSize="20" 
                           FontWeight="Bold" 
                           Margin="0,0,0,10" />
            
            <emoji:TextBlock Text="请选择要执行的战斗脚本，系统将自动转换为AI环境动作指令" 
                           FontSize="14" 
                           Margin="0,0,0,15" 
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

            <!-- 脚本列表 -->
            <emoji:TextBlock Text="📋 可用脚本：" 
                           FontSize="14" 
                           FontWeight="Bold" 
                           Margin="0,0,0,8" />
            
            <Border Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="10"
                    Margin="0,0,0,20">
                
                <ListBox Name="ScriptListBox"
                         Background="Transparent"
                         BorderThickness="0"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         MaxHeight="300">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="8,6" 
                                    Margin="2"
                                    CornerRadius="4"
                                    Background="Transparent">
                                <StackPanel Orientation="Horizontal">
                                    <emoji:TextBlock Text="⚔️" 
                                                   FontSize="14" 
                                                   Margin="0,0,8,0" 
                                                   VerticalAlignment="Center" />
                                    <TextBlock Text="{Binding}" 
                                               FontSize="13" 
                                               VerticalAlignment="Center" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="0,1" />
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource AccentFillColorDefaultBrush}" />
                                    <Setter Property="Foreground" Value="{DynamicResource TextOnAccentFillColorPrimaryBrush}" />
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource SubtleFillColorSecondaryBrush}" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Border>

            <!-- 脚本预览 -->
            <emoji:TextBlock Text="👁️ 脚本预览：" 
                           FontSize="14" 
                           FontWeight="Bold" 
                           Margin="0,0,0,8" />
            
            <Border Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="10"
                    Margin="0,0,0,20">
                
                <TextBox Name="PreviewTextBox"
                         Background="Transparent"
                         BorderThickness="0"
                         IsReadOnly="True"
                         FontFamily="Consolas"
                         FontSize="12"
                         Height="80"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         Text="请选择脚本查看内容..." />
            </Border>

            <!-- 按钮区域 -->
            <StackPanel Orientation="Horizontal" 
                        HorizontalAlignment="Right">
                <ui:Button Name="BtnCancel"
                           Content="取消"
                           Margin="0,0,10,0"
                           Padding="20,8"
                           Click="BtnCancelClick" />
                
                <ui:Button Name="BtnOk"
                           Content="执行脚本"
                           Appearance="Primary"
                           Padding="20,8"
                           IsDefault="True"
                           Click="BtnOkClick" />
            </StackPanel>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
